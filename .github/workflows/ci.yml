name: CI workflow
on: [push, pull_request].

jobs:
    ci-job:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4.1.1
            - name: Run my own composite action
              id: my-action
              uses: ./
              with:
                who-to-greet: "DM"
            
            - name: Outpuut the answerGreeting action
              run: echo "Odpowiedź to ${{ steps.my-action.outputs.answer }}"